@extends('layouts.app')
@section('content')<div class="container">@include('admin.partials.nav')<div class="d-flex justify-content-between"><h4>Dispositivos</h4><a href="{{ route('devices.create') }}" class="btn btn-primary">Nuevo</a></div><table class="table"><tr><th>Nombre</th><th>Serial</th><th>Estado</th><th>API Key</th><th></th></tr>@foreach($devices as $row)<tr><td>{{ $row->name }}</td><td>{{ $row->serial_number }}</td><td>{{ $row->status }}</td><td><code>{{ $row->api_key }}</code></td><td><a href="{{ route('devices.edit',$row) }}" class="btn btn-sm btn-warning">Editar</a><form method="POST" action="{{ route('devices.destroy',$row) }}" class="d-inline" onsubmit="return confirm('¿Eliminar?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Eliminar</button></form></td></tr>@endforeach</table>{{ $devices->links() }}</div>@endsection
