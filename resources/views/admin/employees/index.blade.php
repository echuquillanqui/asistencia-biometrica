@extends('layouts.app')
@section('content')<div class="container">@include('admin.partials.nav')<div class="d-flex justify-content-between"><h4>Empleados</h4><a href="{{ route('employees.create') }}" class="btn btn-primary">Nuevo</a></div><table class="table"><tr><th>Código</th><th>Nombre</th><th>Huella</th><th></th></tr>@foreach($employees as $row)<tr><td>{{ $row->employee_code }}</td><td>{{ $row->first_name }} {{ $row->last_name }}</td><td>{{ $row->fingerprint_id }}</td><td><a href="{{ route('employees.edit',$row) }}" class="btn btn-sm btn-warning">Editar</a><form method="POST" action="{{ route('employees.destroy',$row) }}" class="d-inline" onsubmit="return confirm('¿Eliminar?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Eliminar</button></form></td></tr>@endforeach</table>{{ $employees->links() }}</div>@endsection
