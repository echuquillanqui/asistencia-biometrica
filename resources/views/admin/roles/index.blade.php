@extends('layouts.app')
@section('content')<div class="container">@include('admin.partials.nav')<div class="d-flex justify-content-between"><h4>Roles</h4><a href="{{ route('roles.create') }}" class="btn btn-primary">Nuevo</a></div><table class="table"><tr><th>Nombre</th><th>Permisos</th><th></th></tr>@foreach($roles as $row)<tr><td>{{ $row->name }}</td><td>{{ $row->permissions->pluck('name')->implode(', ') }}</td><td><a href="{{ route('roles.edit',$row) }}" class="btn btn-sm btn-warning">Editar</a><form method="POST" action="{{ route('roles.destroy',$row) }}" class="d-inline" onsubmit="return confirm('¿Eliminar?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Eliminar</button></form></td></tr>@endforeach</table>{{ $roles->links() }}</div>@endsection
