@extends('layouts.app')
@section('content')<div class="container" x-data="{branch:'{{ request('branch_id') }}', type:'{{ request('type') }}'}">@include('admin.partials.nav')<div class="d-flex justify-content-between"><h4>Asistencias</h4><a href="{{ route('attendances.create') }}" class="btn btn-primary">Nueva</a></div><form class="row g-2 my-2"><div class="col"><select name="branch_id" class="form-select" x-model="branch"><option value="">Sucursal</option>@foreach($branches as $b)<option value="{{ $b->id }}">{{ $b->name }}</option>@endforeach</select></div><div class="col"><select name="type" class="form-select" x-model="type"><option value="">Tipo</option><option value="checkin">Entrada</option><option value="checkout">Salida</option></select></div><div class="col"><button class="btn btn-secondary">Filtrar</button></div></form><table class="table"><tr><th>Empleado</th><th>Hora</th><th>Tipo</th><th></th></tr>@foreach($attendances as $row)<tr><td>{{ $row->employee->first_name ?? '' }}</td><td>{{ $row->check_time }}</td><td>{{ $row->type }}</td><td><a href="{{ route('attendances.edit',$row) }}" class="btn btn-sm btn-warning">Editar</a><form method="POST" action="{{ route('attendances.destroy',$row) }}" class="d-inline" onsubmit="return confirm('¿Eliminar?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Eliminar</button></form></td></tr>@endforeach</table>{{ $attendances->links() }}</div>@endsection
