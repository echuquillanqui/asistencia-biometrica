@extends('layouts.app')
@section('content')<div class="container">@include('admin.partials.nav')<div class="d-flex justify-content-between"><h4>Permisos</h4><a href="{{ route('permissions.create') }}" class="btn btn-primary">Nuevo</a></div><table class="table"><tr><th>Nombre</th><th></th></tr>@foreach($permissions as $row)<tr><td>{{ $row->name }}</td><td><a href="{{ route('permissions.edit',$row) }}" class="btn btn-sm btn-warning">Editar</a><form method="POST" action="{{ route('permissions.destroy',$row) }}" class="d-inline" onsubmit="return confirm('¿Eliminar?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Eliminar</button></form></td></tr>@endforeach</table>{{ $permissions->links() }}</div>@endsection
